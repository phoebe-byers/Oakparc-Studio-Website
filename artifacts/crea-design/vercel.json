{
  "buildCommand": "pnpm --filter @workspace/oakparc-studio build",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
git add .
git commit -m "move vercel.json to crea-design"
git push

